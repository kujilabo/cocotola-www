import React, { useEffect } from 'react';
// import { useParams } from 'react-router-dom';
// import { Card, Container, Divider, Header } from 'semantic-ui-react';

import { useAppSelector } from '../../../../../../app/hooks';
// import { findProblems, startStudy } from '../../../../../../actions/study';
// import { EnglishWordMemorizationBreadcrumb } from './EnglishWordMemorizationBreadcrumb';
//import { ErrorMessage, AppDimmer } from '../../../../../../components';
import { AppDimmer } from 'components';
// import { AppState } from '../../../../../../reducers/index';
import { WorkbookModel } from 'models/workbook';
import { RecordbookModel } from 'models/recordbook';
import { selectTs } from '../../../../features/english_word_study';
import 'App.css';

// type ParamTypes = {
//   _workbookId: string;
// };
const EnglishWordMemorizationInit: React.FC<EnglishWordMemorizationInitProps> = (
  props: EnglishWordMemorizationInitProps
) => {
  // const { _workbookId } = useParams<ParamTypes>();
  // const [ts, setTs] = useState('');
  const ts = useAppSelector(selectTs);

  useEffect(() => {
    console.log('english word init');
    return;
  }, [ts]);

  if (!props.workbook) {
    return <AppDimmer />;
  }

  console.log('record', props.recordbook);
  // if (props.workbook.problems.length === 0) {
  //   return (
  //     <Container fluid>
  //       <EnglishWordMemorizationBreadcrumb
  //         name={props.workbook.name}
  //         id={+_workbookId}
  //       />
  //       <Divider hidden />
  //       <Card className="full-width">
  //         <Card.Content>
  //           <Header component="h2">Problems not found</Header>
  //         </Card.Content>
  //       </Card>
  //       {/* <ErrorMessage message={props.errorMessage} /> */}
  //     </Container>
  //   );
  // }

  // props.startStudy('');
  return <div></div>;
};

type EnglishWordMemorizationInitProps = {
  loading: boolean;
  failed: boolean;
  workbook: WorkbookModel;
  recordbook: RecordbookModel;
  index: number;

  startStudy: (a: string) => void;
};

// export default connect(
//   (state: AppState) => ({
//     loading: state.study.loading,
//     failed: state.study.failed,
//     index: state.study.index,
//     workbook: state.workbook.workbook,
//     recordbook: state.study.recordbook,
//   }),
//   {
//     startStudy,
//   }
// )(EnglishWordMemorizationInit);
